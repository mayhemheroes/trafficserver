meta:
  version: "1.0"

sessions:
- transactions:
  - all: { headers: { fields: [[ uuid, success ]]}}
    client-request:
      method: "GET"
      version: "1.1"
      scheme: "http"
      url: /index.html
      headers:
        fields:
        - [ Host, charlie.ex ]

    proxy-response:
      status: 404
